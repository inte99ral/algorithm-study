#include <regex>
#include <typeinfo>
#include <vector>

#include "Test.hpp"

using namespace std;

int main() {
  {
    // vector<string> file_names = {"db-123-log.txt",
    //                              "db-124-log.txt",
    //                              "not-db-log.txt",
    //                              "db-12-log.txt",
    //                              "db-12-log.jpg"};

    // regex regExp("db-\\d*-log\\.txt");
    // for (const auto &file_name : file_names) {
    //   cout << file_name << ": " << boolalpha << regex_match(file_name, regExp) << '\n';
    // }
  }

  {
    // ArrayParameter ap = ArrayParameter();
    // int iArr[4] = {100, 101, 102, 103};
    // ap.idCheck(iArr);
    // ap.idCheck(iArr);
  }

  return 0;
}
