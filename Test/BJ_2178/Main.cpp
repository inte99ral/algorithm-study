#include <bits/stdc++.h>
using namespace std;

void perm(int depth, int N, int R) {
    if (depth == R) {
        return;
    }
    return;
}

int main() {
    int N = 4;
    int R = 3;
    vector<int> origin(N);
    for(int n = 0; n < N; n++) origin[n] = n + 1;
    
    return 0;
}